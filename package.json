{
  "name": "tender-checklist-generator-root",
  "version": "1.0.0",
  "description": "Root configuration for running backend and frontend concurrently",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "dev-backend": "npm run start --prefix backend",
    "dev-frontend": "npm run dev --prefix frontend",
    "start": "npx concurrently \"npm run dev-backend\" \"npm run dev-frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
